<Project Sdk="Microsoft.NET.Sdk">
  <PropertyGroup>
    <OutputType>Exe</OutputType>
    <TargetFramework>net48</TargetFramework>
    <RootNamespace>SpellFire.RobotManager.Cli</RootNamespace>
    <AssemblyName>SpellFire.RobotManager.Cli</AssemblyName>
    <GenerateAssemblyInfo>false</GenerateAssemblyInfo>
    <PlatformTarget>x86</PlatformTarget>
    <Prefer32Bit>true</Prefer32Bit>
  </PropertyGroup>

  <ItemGroup>
    <ProjectReference Include="..\SpellFire.RobotManager\SpellFire.RobotManager.csproj" />
    <ProjectReference Include="..\SpellFire.WowRuntime\SpellFire.WowRuntime.csproj" />
  </ItemGroup>

  <Target Name="CopySpellFireHookPayload" AfterTargets="Build">
    <ItemGroup>
      <SpellFireHookPayload Include="..\SpellFire.Hook\bin\$(Configuration)\SpellFire.Hook.dll" />
    </ItemGroup>
    <Copy SourceFiles="@(SpellFireHookPayload)"
          DestinationFiles="@(SpellFireHookPayload->'$(OutputPath)SpellFire.Hook.source.dll')"
          SkipUnchangedFiles="true"
          Condition="Exists('%(SpellFireHookPayload.FullPath)')" />
  </Target>
</Project>
